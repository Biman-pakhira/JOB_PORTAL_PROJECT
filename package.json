{
  "name": "job-portal-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix backend & npm run dev --prefix frontend",
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && npx prisma generate && cd .. && cp -r frontend/dist/* ."
  },
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.1",
    "express": "^5.2.1",
    "google-auth-library": "^10.6.2",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.1.1",
    "prisma": "^6.19.3",
    "xlsx": "^0.18.5"
  }
}
